name: PR Auto-format

# This workflow triggers when a PR is opened/updated
on:
  pull_request_target:
    types: [opened, synchronize, reopened]
    branches:
      - main
      - release

concurrency:
  group: pr-fmt-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  auto_format:
    if: |
      !contains(github.event.pull_request.labels.*.name, 'skip:ci') &&
      !contains(github.event.pull_request.head.sha, '[skip ci]')
    permissions:
      contents: write
      pull-requests: write
      checks: read
    name: Auto-format PR code
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      # Wait for CI checks to appear and complete
      # Uses check runs (not workflow runs) to avoid action_required approval issues
      # Waits for actual CI job checks like rust_tests, lint, etc.
      - name: Wait for CI checks to complete
        uses: lewagon/wait-on-check-action@v1.3.4
        timeout-minutes: 120
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          wait-interval: 30
          running-workflow-name: 'Auto-format PR code'
          check-regexp: '^(rust_tests|exotic_targets|snippets_cpython|lint|miri|wasm|wasm-wasi)'
          allowed-conclusions: success,skipped,neutral
          verbose: true

      - name: CI completed successfully
        run: echo "CI workflow completed successfully - proceeding with auto-format"

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Run cargo fmt
        run: |
          echo "Running cargo fmt --all on PR #${{ github.event.pull_request.number }}"
          cargo fmt --all

      - name: Check for formatting changes
        id: check_changes
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
          else
            echo "has_changes=false" >> $GITHUB_OUTPUT
          fi

      - name: Commit and push formatting changes
        if: steps.check_changes.outputs.has_changes == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          git add -u
          git commit -m "Auto-format code [skip ci]"
          
          git push origin HEAD:${{ github.event.pull_request.head.ref }}

      - name: Comment on PR
        if: steps.check_changes.outputs.has_changes == 'true'
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          number: ${{ github.event.pull_request.number }}
          message: |
            **Code has been automatically formatted**
            
            The code in this PR has been formatted using `cargo fmt`.
            The changes have been committed with `[skip ci]` to avoid triggering another CI run.
            
            You may need to pull the latest changes before pushing again:
            ```bash
            git pull origin ${{ github.event.pull_request.head.ref }}
            ```

      - name: No formatting needed
        if: steps.check_changes.outputs.has_changes == 'false'
        run: echo "Code is already properly formatted"
